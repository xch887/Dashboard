# Live Intelligence / Operations copilot (https://platform.openai.com/api-keys)
# Leave unset to use built-in demo responses (same UI, no API calls).
OPENAI_API_KEY=

# Optional override (default: gpt-4o-mini)
# OPENAI_OPERATIONS_MODEL=gpt-4o-mini
